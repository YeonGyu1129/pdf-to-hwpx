# 📐 수학 문제 → 한글(HWPX) 변환기 사용설명서

Claude Vision AI 로 수학 문제 이미지·PDF 를 인식해서
**한글(HWPX) 파일**로 변환하는 Streamlit 웹 앱입니다.

---

## 🌐 바로 쓰기 (가장 쉬운 방법)

브라우저에서 아래 주소로 접속하면 별도 설치 없이 바로 사용 가능합니다.

> **https://yeongyu-pdf-to-hwpx.streamlit.app**

맥·윈도우·모바일 어디서든 열립니다.

---

## ✨ 할 수 있는 일

| 입력 | 처리 | 출력 |
|------|------|------|
| 📷 이미지(JPG/PNG/WEBP) | Claude Vision 으로 수식·문제 인식 | 한글 HWPX 파일 |
| 📄 PDF (여러 페이지 OK) | 페이지별 처리 후 병합 | 다운로드 버튼 |
| 📋 클립보드 붙여넣기 | 스크린샷 바로 처리 | |

### 자동 처리되는 수식 규칙
- ✅ **점·선분** (`A(1,0)`, `\overline{AB}`, `\triangle OAB`) → 로만체
- ✅ **프라임** (`O'`, `A'`) → 공백 + `prime` 키워드
- ✅ **변수** (`x`, `y`, `a`, `b`) → 이탤릭체
- ✅ **그리스문자** (`α_1, β, θ`) → 정상 렌더
- ✅ **집합 기호** (합집합 `∪`, 교집합 `∩`)
- ✅ **극한 방향** (`x→2^-` → `x→2-`)
- ✅ **삼각함수** (`\sin`, `\cos`) 자동 보정
- ✅ **객관식 보기** (①②③④⑤) 자동 줄바꿈
- ✅ **문제 간 간격** 자동 조정 (2줄 공백)

---

## 📖 사용 순서

### 1️⃣ 문제 업로드

**방법 A — 파일 업로드**
- 상단 "문제 파일 업로드" 영역에 **드래그** 또는 **클릭해서 선택**
- 여러 파일을 한 번에 올릴 수 있음
- 지원 형식: **PDF, JPG, JPEG, PNG, WEBP**

**방법 B — 클립보드 붙여넣기** (스크린샷 바로 처리)

| OS | 캡처 단축키 |
|----|------------|
| 맥 | `Ctrl + Cmd + Shift + 4` → 영역 선택 → 클립보드 복사 |
| 윈도우 | `Win + Shift + S` → 영역 선택 → 클립보드 복사 |

그런 다음 **앱 화면에서 `Cmd+V` (맥) / `Ctrl+V` (윈도우)** 또는
**"📋 클립보드에서 붙여넣기"** 버튼 클릭

- 여러 번 연속 붙여넣기 가능
- 썸네일이 그려져서 어떤 이미지가 추가됐는지 확인 가능
- **🗑 비우기** 버튼으로 전체 삭제

### 2️⃣ 템플릿 (선택)

`template.hwpx` 가 기본으로 설정돼 있어요. 다른 템플릿을 쓰려면 아래 업로드 영역에 HWPX 파일 업로드.

### 3️⃣ 🚀 변환 시작

버튼을 누르면 다음 단계가 순서대로 진행됩니다:

1. **입력 파일 처리** (PDF 를 이미지로 렌더링)
2. **Claude Vision 인식** (페이지별 진행률 표시)
3. **문제 구조화** (JSON 형태로 정리)
4. **HWPX 생성**

각 단계 진행 상황과 완료 여부가 화면에 표시됩니다.

### 4️⃣ ⬇️ 다운로드

완료되면 **⬇️ HWPX 다운로드** 버튼이 나타납니다.
파일명은 `converted.hwpx`.

---

## ⚙️ 사이드바 설정

왼쪽 사이드바를 펼치면 고급 설정이 있습니다.

| 설정 | 기본값 | 설명 |
|------|--------|------|
| **PDF 렌더링 DPI** | 150 | PDF → 이미지 변환 해상도. 올리면 인식 정확도↑ 속도↓ |
| **Vision 인식 원문 보기** | 꺼짐 | 디버그용. 어떤 LaTeX 텍스트가 나왔는지 확인 |
| **구조화 JSON 보기** | 꺼짐 | 디버그용. HWPX 생성 직전의 JSON 구조 확인 |

---

## 🖥️ 로컬에서 실행 (선택)

인터넷 없이 본인 컴퓨터에서 돌리려면:

### 맥

```bash
# 1. 폴더 이동
cd "~/클로드 코드 작업/pdf를 hwpx로 변환"

# 2. 가상환경 활성화
source .venv/bin/activate

# 3. 첫 실행 시 의존성 설치
pip install -r requirements.txt
brew install poppler    # PDF 처리용

# 4. API 키 설정
export ANTHROPIC_API_KEY="sk-ant-..."

# 5. 실행
streamlit run app.py
```

또는 더 간단히 — **`run.command` 파일 더블클릭**
(`.env` 파일에 `ANTHROPIC_API_KEY=sk-ant-...` 저장해두면 자동 로드됨)

브라우저에서 `http://localhost:8501` 로 접속

### 윈도우

```cmd
cd "폴더 경로"
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
set ANTHROPIC_API_KEY=sk-ant-...
streamlit run app.py
```

윈도우는 추가로 [Poppler for Windows](https://github.com/oschwartz10612/poppler-windows/releases) 설치 필요.

---

## 🔧 자주 묻는 질문

### Q1. 변환이 오래 걸려요
**A.** 페이지별로 Claude API 호출하므로 페이지 수에 비례합니다.
- 1페이지: 약 10~20초
- 10페이지: 약 2~3분

### Q2. API 키는 어디서 받나요?
**A.** [console.anthropic.com](https://console.anthropic.com) → 로그인 → **Settings → API Keys → Create Key**

### Q3. 비용이 얼마나 드나요?
**A.** Claude Opus 4.5 모델 기준 페이지당 약 **$0.02~0.05**. 월 50페이지 변환 시 약 $1~3 수준.

### Q4. 수식이 이상하게 나와요
**A.** 사이드바에서 **"Vision 인식 원문 보기"** / **"구조화 JSON 보기"** 를 켜서:
- Vision 원문에 수식이 **있는데** 결과에 없음 → 구조화 단계 문제
- Vision 원문에 **아예 없음** → Vision 이 인식 실패 (DPI 를 올리거나 해당 페이지만 잘라서 재시도)

### Q5. 템플릿을 바꾸고 싶어요
**A.** 한글에서 원하는 스타일의 빈 HWPX 를 만들어 저장 → 변환 시 "HWPX 템플릿 업로드" 영역에 첨부

### Q6. 결과물에서 글자 크기·폰트를 바꾸고 싶어요
**A.** 앱은 `template.hwpx` 의 스타일을 따릅니다. 한글에서 템플릿을 열어 **문단 모양 / 글꼴** 을 미리 설정해두세요.

### Q7. 스크린샷 붙여넣기가 안 돼요
**A.** 브라우저가 클립보드 권한을 묻는 팝업을 **허용** 해주세요. Chrome 이 가장 안정적입니다. HTTPS 필요 (Streamlit Cloud 는 자동 HTTPS).

### Q8. 문제 사이 간격을 조정하고 싶어요
**A.** `app.py` 에서 `PROBLEM_GAP_LINES = 2` 값을 바꾸면 됩니다. (로컬 실행 시)

---

## 🛠️ 트러블슈팅

### "ANTHROPIC_API_KEY 환경변수가 없습니다" 에러
로컬 실행 시 API 키를 설정 안 한 경우. `export ANTHROPIC_API_KEY="sk-ant-..."` 실행.

### "RequestTooLargeError"
이미지가 너무 큼. 자동 리사이즈 기능이 있지만 초대형 이미지면 실패 가능. 사이드바에서 DPI 를 낮추거나 이미지를 미리 줄여서 업로드.

### "ModuleNotFoundError: No module named 'pdf_to_hwpx'"
프로젝트 폴더에 `pdf_to_hwpx.py` 가 없음. 원본 스크립트를 app.py 옆에 복사.

### 변환은 됐는데 수식이 깨져 보임
- 결과 HWPX 를 한글에서 열어 **수식 더블클릭** → 하단 스크립트 창에서 hwpEQ 코드 확인
- 특정 패턴이 반복되면 프롬프트/후처리를 추가 보정 가능

---

## 📂 파일 구조

```
pdf를 hwpx로 변환/
├── app.py                  # Streamlit 메인 앱
├── pdf_to_hwpx.py          # HWPX 빌더 라이브러리
├── template.hwpx           # 기본 스타일 템플릿
├── requirements.txt        # Python 의존성
├── packages.txt            # 시스템 패키지 (poppler)
├── run.command             # 맥용 원클릭 실행 스크립트
├── .env.example            # API 키 설정 예시
├── README.md               # 개발자용 문서
└── 사용설명서.md           # ← 이 파일
```

---

## 📝 수정·배포 흐름

코드 수정 후 변경사항 반영:

```bash
# 1. 수정
cd "~/클로드 코드 작업/pdf를 hwpx로 변환"
# (코드 편집)

# 2. 로컬 테스트
streamlit run app.py

# 3. GitHub 에 push
git add .
git commit -m "수정 내용"
git push

# 4. Streamlit Cloud 가 1~2분 내 자동 재배포
```

---

## 🔐 보안

- **API 키는 서버에만 저장** — Streamlit Secrets 에서 관리, 사용자는 키 볼 수 없음
- `.env`, `secrets.toml` 은 Git 제외 (`.gitignore` 설정됨)
- 업로드한 이미지는 처리 후 임시폴더에 저장됐다가 **자동 삭제**

---

## 🎁 부록 — 지원하는 LaTeX 패턴

| LaTeX | 출력 | 설명 |
|-------|------|------|
| `f(x)`, `x^2` | 기본 수식 | 이탤릭 자동 |
| `\frac{a}{b}` | 분수 | |
| `\sqrt{x}` | 루트 | |
| `\sum_{k=1}^{n}` | 합 | |
| `\int_a^b` | 적분 | |
| `\lim_{x \to a}` | 극한 | |
| `\lim_{x \to a^-}`, `\lim_{x \to a^+}` | 좌/우 극한 | `a-`, `a+` 로 렌더 |
| `\sin`, `\cos`, `\tan` | 삼각함수 | |
| `\log`, `\ln` | 로그 | |
| `\alpha, \beta, \theta, ..., \Omega` | 그리스 문자 | |
| `\mathrm{A}`, `\mathrm{AB}` | 로만체 | 점·선분용 |
| `\overline{AB}`, `\overrightarrow{AB}` | 선분·반직선 | |
| `\triangle ABC`, `\angle ABC` | 삼각형·각 | |
| `A \cup B`, `A \cap B` | 합집합·교집합 | |
| `\{x \mid x > 0\}` | 집합 표기 | |

---

## 💬 문의

수정 요청이나 버그 발견 시:
- 원본 이미지 + 변환 결과 스크린샷
- 필요하면 한글 수식편집기의 hwpEQ 코드

를 함께 공유하면 빠르게 대응 가능합니다.

---

*최종 업데이트: 2026-04-18*
